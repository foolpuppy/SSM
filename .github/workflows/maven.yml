name: Java CI

on:
schedule:
- cron:  */15 * * * *

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: 设置JDK版本为1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: Maven打包
      run: mvn package --file mall/pom.xml
